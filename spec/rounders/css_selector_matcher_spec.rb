require 'spec_helper'

describe Rounders::CssSelectorMatcher do
  it 'has a version number' do
    expect(Rounders::CssSelectorMatcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
